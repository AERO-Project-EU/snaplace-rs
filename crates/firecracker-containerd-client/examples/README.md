# Examples

> **Note**:
> Some hard-coded filesystem paths (e.g., in [snap.rs](snap.rs) and
> [ns.rs](ns.rs)) assume CWD to be the workspace's root directory.

```
# RUST_LOG=snap=trace target/debug/examples/snap
```
